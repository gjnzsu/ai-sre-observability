apiVersion: v1
kind: Service
metadata:
  name: ai-sre-observability
  namespace: default
  labels:
    app: ai-sre-observability
spec:
  type: ClusterIP
  selector:
    app: ai-sre-observability
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
